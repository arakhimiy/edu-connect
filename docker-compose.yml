version: '3.8'

services:
  main-service:
    image: dockerhub/image:latest # replace with your own image
    container_name: voyager
    ports:
      - "8090:8090"
    volumes:
      - pb_data:/pb_data
      - migrations:/migrations
    restart: always

volumes:
  pb_data:
  migrations: